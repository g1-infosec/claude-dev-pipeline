# new-agent

How to add a stage to a multi-agent pipeline the product itself runs (fan-out of specialist agents).

- **Included when:** you're building a pipeline product, not for typical apps.
- Covers the shared client, typed output schema, the prompt-version → cache-invalidation rule, and registering in the coordinator.

Details in [`SKILL.md`](./SKILL.md).
